// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyD_-Mvsc06TIB9643QNP4JqPqggkuulPz8",
    authDomain: "chaletuls.firebaseapp.com",
    projectId: "chaletuls",
    storageBucket: "chaletuls.appspot.com",
    messagingSenderId: "297799084751",
    appId: "1:297799084751:web:d6fa8ab69143abce56a5ba",
    measurementId: "G-ZL8YVF38MC"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
